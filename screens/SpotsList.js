import React from "react";

import ListComponent from "../components/ListComponent/ListComponent";

export default function SpotsList({ navigation }) {
  return <ListComponent navigation={navigation} />;
}
